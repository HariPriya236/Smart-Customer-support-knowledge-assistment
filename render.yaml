services:
  # Backend Web Service (FastAPI + LangGraph)
  - type: web
    name: supportiq-backend
    env: python
    rootDir: backend
    buildCommand: pip install --no-cache-dir -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DEFAULT_LLM_PROVIDER
        value: gemini
      # Use Gemini embeddings API (no local model = no PyTorch = no OOM)
      - key: EMBEDDING_PROVIDER
        value: gemini
      - key: DATABASE_URL
        value: sqlite:////data/supportiq.db
      - key: CHROMA_PERSIST_DIR
        value: /data/chroma_db
      - key: GEMINI_API_KEY
        sync: false # Set this in the Render Dashboard
    disk:
      name: supportiq-data
      mountPath: /data
      sizeGB: 1

  # Frontend Static Site (React + Vite)
  - type: static
    name: supportiq-frontend
    env: node
    rootDir: frontend
    buildCommand: npm install && npm run build
    publishPath: dist
    envVars:
      - key: VITE_API_BASE_URL
        value: https://supportiq-backend.onrender.com/api
